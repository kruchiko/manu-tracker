# Base URL for the backend API.
# Leave empty to use the Vite dev-server proxy (/orders → http://localhost:3000).
# In production, set this to the full API origin, e.g. https://api.example.com
VITE_API_URL=
