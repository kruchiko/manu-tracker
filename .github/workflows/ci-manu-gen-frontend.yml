name: CI — manu-gen frontend

on:
  push:
    branches: [main]
    paths: [manu-gen/frontend/**]
  pull_request:
    branches: [main]
    paths: [manu-gen/frontend/**]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: manu-gen/frontend

    steps:
      - uses: actions/checkout@v4

      - run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version-file: manu-gen/frontend/.nvmrc
          cache: yarn
          cache-dependency-path: manu-gen/frontend/yarn.lock

      - run: yarn install --immutable

      - run: yarn lint
      - run: yarn typecheck
      - run: yarn build
      - run: yarn test
